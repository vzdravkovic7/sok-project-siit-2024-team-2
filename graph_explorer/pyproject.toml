[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "graph-graph_explorer"
version = "0.1"
description = "Graph web application."
dependencies = [
    "platform==0.1",
    "Django>=5.1"
]