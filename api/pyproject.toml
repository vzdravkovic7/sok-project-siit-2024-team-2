[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "graph-api"
version = "0.1"
description = "Graph API for the project."
dependencies = [
    "Django>=5.1.3",
    "djangorestframework"
]

[tool.setuptools]
provides = ["api"]
